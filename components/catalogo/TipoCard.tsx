'use client'

import type { TipoCortina } from '@/types'

interface TipoCardProps {
  tipo: TipoCortina
  onClick?: () => void
}

export default function TipoCard({ tipo, onClick }: TipoCardProps) {
  // Card de categoría de cortina para el catálogo
  return <div />
}