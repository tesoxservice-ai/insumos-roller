'use client'

import type { ConfiguradorState } from '@/types'

interface ProbadorVisualProps {
  state: ConfiguradorState
}

export default function ProbadorVisual({ state }: ProbadorVisualProps) {
  // Preview visual de la cortina con el color hex seleccionado
  return <div />
}