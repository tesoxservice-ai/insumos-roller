'use client'

interface SimuladorLuzProps {
  telaId: string | null
}

export default function SimuladorLuz({ telaId }: SimuladorLuzProps) {
  // Simulación del nivel de oscurecimiento según la tela seleccionada
  return <div />
}