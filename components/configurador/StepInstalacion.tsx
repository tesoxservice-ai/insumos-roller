'use client'

interface StepInstalacionProps {
  instalacion: boolean
  onChange: (activa: boolean, extra: number) => void
}

export default function StepInstalacion({ instalacion, onChange }: StepInstalacionProps) {
  // Paso 6: opción de instalación profesional incluida
  return <div />
}