'use client'

import type { ItemPresupuesto } from '@/types'

interface PDFGeneratorProps {
  items: ItemPresupuesto[]
}

export default function PDFGenerator({ items }: PDFGeneratorProps) {
  // Botón que dispara la generación y descarga del PDF del presupuesto
  return <div />
}